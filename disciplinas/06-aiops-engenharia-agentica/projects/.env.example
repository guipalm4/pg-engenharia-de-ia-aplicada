# Copie para .env na RAIZ de projects/ (um único arquivo serve todos os projetos:
# core/llm_config.py usa load_dotenv(), que sobe a árvore de diretórios até achar).
#
#   cp .env.example .env
#
# Chave da Groq: https://console.groq.com/keys
GROQ_API_KEY=
